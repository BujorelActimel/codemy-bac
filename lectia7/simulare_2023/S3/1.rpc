citeste x, y

rez <- 0

pentru i <- x, y repeta
    c <- 0
    pentru j <- 1, i, 2 repeta
        daca i % j = 0 atunci
            c <- c + 1
        sf
    sf
    daca c = 3 atunci
        scrie i
        rez <- rez + 1
    sf
sf

scrie 'Rezultat:', rez
